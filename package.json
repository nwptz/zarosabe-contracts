{
    "name": "zarosabe-contracts",
    "version": "0.1.0",
    "private": true,
    "description": "Professional smart contract repository baseline for Zarosabe contracts.",
    "scripts": {
        "format": "prettier --write .",
        "format:check": "prettier --check .",
        "lint": "solhint \"contracts/**/*.sol\"",
        "build": "forge build",
        "test": "forge test -vv"
    },
    "devDependencies": {
        "prettier": "^3.4.2",
        "prettier-plugin-solidity": "^1.4.2",
        "solhint": "^5.0.5"
    }
}
